读取 manifest.json，生成 doc.md：每个 entry 一个二级标题章节，标题格式 `N. id`（N 按数组顺序从 1 开始编号）；章节正文一行 `See also: §X, §Y`，X/Y 是 rels 里各 id 对应的章节编号，按 rels 数组顺序、逗号加空格分隔；rels 为空数组的章节不写这行。每个章节标题行与正文行之间空一行。文件顶部是一级标题 `# Index` 和一个有序目录列表（每项一行，`N. id`），目录之后空一行再接第一个章节；各章节之间空一行（即上一个章节的正文行之后空一行再接下一个章节标题）。文件以换行结尾。
