读取 products.tsv（Tab 分隔，首行是表头），生成 total.md：一个 Markdown 表格，四列依次为 名称、单价、数量、小计；每行以 `|` 开头和结尾，列之间用 ` | ` 分隔（如 `| 铅笔 | 2 | 10 | 20 |`）；每个商品一行，小计 = 单价 × 数量；最后一行总计，名称为 总计、小计列为三个商品小计之和（单价和数量两列留空）。保留中文原文，标准分隔行（`| --- | --- | --- | --- |`），文件不要带 BOM，以换行结尾。
